import { View, Text, StyleSheet, ImageBackground } from 'react-native';
import React from 'react';

export default function Interfaz2() {
  return (
    <Text>Works</Text>
  )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
    },
    image: {
        flex: 1,
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
    },
});